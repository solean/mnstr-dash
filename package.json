{
  "name": "mnstr",
  "version": "1.0.0",
  "description": "",
  "main": "server.js",
  "scripts": {
    "start": "node server.js",
    "dev": "nodemon server.js",
    "dev:client": "bun build src/main.jsx --outdir=dist --target=browser --watch --sourcemap --splitting",
    "build": "bun build src/main.jsx --outdir=dist --target=browser --minify --sourcemap --splitting",
    "test": "echo \"Error: no test specified\" && exit 1"
  },
  "keywords": [],
  "author": "",
  "license": "ISC",
  "type": "commonjs",
  "dependencies": {
    "@vercel/analytics": "^1.6.1",
    "chart.js": "^4.5.1",
    "express": "^5.2.1",
    "react": "^19.2.1",
    "react-dom": "^19.2.1"
  },
  "devDependencies": {
    "nodemon": "^3.1.11"
  }
}
